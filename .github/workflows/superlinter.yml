name: Super-Linter

on: push


jobs:
   super-lint:
       name: Lint code base
       runs-on: ubuntu-latest
       steps:
          - name: Checkout code
             run-name: Deploy to ${{ inputs.deploy_target }} by @${{ github.actor }}

